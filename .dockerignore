# Keep build contexts small and avoid copying host-built artifacts into images.
**/node_modules
**/.next
**/dist
**/.turbo
.git
**/*.log
# Never bake local secrets into images — compose injects .env via env_file at runtime.
.env

